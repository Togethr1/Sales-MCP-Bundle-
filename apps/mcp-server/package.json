{
  "name": "@sales-mcp/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "demo:account-status": "tsx src/index.ts",
    "dev:stdio": "tsx src/stdio.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "@sales-mcp/core": "workspace:*",
    "@sales-mcp/providers": "workspace:*",
    "@sales-mcp/workflows": "workspace:*",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
